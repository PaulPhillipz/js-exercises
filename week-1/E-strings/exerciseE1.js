// Start by creating a variable `message`
var message_one = "This is a string fit for a King";
var messageType = typeof message_one;

console.log(message_one);
console.log(typeof message_one);
console.log(messageType); // should show 'string'
